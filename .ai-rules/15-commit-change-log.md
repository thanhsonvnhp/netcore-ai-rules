---
name: change-log-documentation
description: Write changelog entries for code changes. Use when the Agent changes or reviews API endpoints, request/response contracts, validation behavior, database scripts or schema, shared building blocks, auth behavior, configuration, deployment behavior, background jobs, or event flow and needs to add or update `change-logs/YYYY/MM/YYYY-MM-DD.md`. MUST use `change-log-documentation` skill.
---

# Change Log Writer

Use `change-log-documentation` at `.agents/change-log-documentation` for the rules on how to write change logs.
